pipeline {
    agent {
        label 'slave'
    }
    stages {
        stage('Build') {
            steps {
                sh 'hostname'
                sh 'who -a'
            }
        }
        
    }
}
